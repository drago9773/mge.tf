<h1 class="description">TF2's first ever MGE league</h1>
<div class="placeholder">
    <table>
        <tr class="elo-title">
            <th>Rank</th>
            <th>Name</th>
            <th>Elo</th>
            <th>Wins</th>
            <th>Losses</th>
        </tr>
        <% elo.forEach((player, index) => { %>
            <tr class="elo-row">
                <td><%= index + 1 %></td>
                <td><%= player.name %></td>
                <td><%= player.rating %></td>
                <td><%= player.wins %></td>
                <td><%= player.losses %></td>
            </tr>
        <% }) %>
    </table>
</div>
